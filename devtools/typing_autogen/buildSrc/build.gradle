apply plugin: 'groovy'
apply plugin:'java'

repositories {
    //maven { url 'http://repo.gradle.org/gradle/libs' }
    //mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}